import { HttpResponseHandler } from './http-response-handler';

describe('ResponseHandler', () => {
  it('should create an instance', () => {
    expect(new HttpResponseHandler()).toBeTruthy();
  });
});
